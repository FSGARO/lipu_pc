import axios from '@/api/http'
import base from '@/api/base'
import seifinspection from '@/api/seifinspection'
import login from '@/api/login'
import controltask from '@/api/controltask'
// import broadcast from '@/api/broadcast';
import terminal from '@/api/terminal'
import operation from '@/api/operation'
import addcontrol from '@/api/addcontrol'
import newctask from '@/api/newctask'
import mediafile from '@/api/mediafile'
import videopreview from '@/api/videopreview'
import powerstats from '@/api/powerstats'
import position from '@/api/position'
import usermanagement from '@/api/usermanagement'
import config from '@/api/config'
import timetable from '@/api/timetable'
import curriculum from '@/api/curriculum'
import cardlist from '@/api/cardlist'
import reserve from '@/api/reserve'

export default {
	axios,
	base,
	seifinspection,
	login,
	controltask,
	terminal,
	operation,
	addcontrol,
	newctask,
	mediafile,
	videopreview,
	powerstats,
	position,
	usermanagement,
	config,
	timetable,
	curriculum,
	cardlist,
	reserve
}
